{
  "name": "bird-id-evidence-card",
  "version": "1.0.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-routes.mjs && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "test:unit": "vitest run",
    "test:build": "node scripts/verify-build.mjs",
    "test:e2e": "node scripts/run-e2e.mjs",
    "test:live": "node scripts/verify-live.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "npm run test:unit && npm run build && npm run test:build && npm run test:e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "@types/node": "^22.18.0",
    "sharp": "0.35.4",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
