# Polish 4 — cumulative finding closure map

Scope: release candidate `c09035ce58bcd5cf9c7fe106664972daf6198d4e`, reviewed through `14e1365a31988ed2b16146202a7918afc089fb09`. Product implementation verified at `ff0078b767cffde6bc6755080f6543a33c0bd395`. No finding is deferred.

Live URL: <https://bird-id-evidence-card.sociobot.in/>. The final cold audit is [live-audit.json](evidence/polish-4/live/live-audit.json).

| Finding | Change made | Evidence |
| --- | --- | --- |
| R1-B1 — unclear first screen | Retained the six-word job headline, birder/situation sentence, one-click sample action, adjacent outcome, and three plain facts in the 390 px first screen. | `first phone screen states the job, audience, action, and outcome`; [home screenshot](evidence/polish-4/live/home-mobile.png); live `/` first-viewport check. |
| R1-B2 — no isolated sample | Retained `?demo=1` and `/demo`, the completed Deerness coast sample, persistent banner, reset/exit controls, cancelled pending saves, and the separate `demo:bird-id-evidence-card` database. | `@claim:demo-isolation`; `@claim:offline-demo`; [demo screenshot](evidence/polish-4/live/demo-mobile.png); live real-data sentinel stayed byte-for-byte unchanged. |
| R1-B3 — missing claims | Kept the 15-entry registry and one unique observable test per claim; updated the artwork claim to match the new plain footer copy. | `maps every claim to exactly one tagged test`; all 15 exact commands passed independently in clean clone `/tmp/bird-polish4-clean.8zNA83`. |
| R1-B4 — fake routes and 200 fallback | Retained static `/demo`, `/records`, and `/guide` documents, History API navigation, route focus/announcement, and the styled host-level 404. | `route responses and rendered pages contain one route-specific h1`; `routes use meaningful titles and navigation restores focus`; live unknown URL returned the designed 404 with HTTP 404. |
| R1-H5 — incomplete metadata and chrome | Kept route-specific descriptions, canonicals, OG/Twitter fields, SVG/apple icons, social art, and the shared shell. Added the full set to `/offline.html` and set build `v1.0.4`. | `all public pages share navigation, footer links, build id, and complete metadata`; live audit covers seven 200 routes plus 404. |
| R1-M6 — jargon and inconsistent terms | Removed remaining visitor-facing `field console`, `archive`, `IDs`, and `locally` language. State copy now uses `evidence card`, `backup`, and `this device`. | `audits dialog, toast, error, offline, and import or export state copy`; `.factory/copy-audit.md`; [dialog screenshot](evidence/polish-4/local/new-card-dialog-mobile.png). |
| R1-M7 — unclear headings and actions | Dialog headings now name the action, and the update action is `Load update`. Existing result-naming route and form actions remain. | `state messages use evidence-card terms and action-specific dialog headings`; route-focus test; live axe/focus audit. |
| R2-B1 — claim commands fail in a clean clone | Preserved the self-building Playwright command path and re-ran every registry command independently from a clone with no pre-existing `dist/`. | 15/15 exact commands passed at `ff0078b`; final clean `npm test` also passed. |
| R2-B2 — route destination remains off-screen | Retained route-specific first content, scroll-to-top, focused h1, and Back/Forward restoration. | `routes use meaningful titles and navigation restores focus`; live keyboard and history check. |
| R2-H3 — missing claim entries | Retained observable coverage for recording, separation, non-identification, accounts, storage schema, deletion, privacy, and artwork. | `.factory/claims.json`; 15/15 exact clean-clone commands passed; live same-origin/no-storage audit passed. |
| R2-H4 — weak demo isolation | Retained the seeded real sentinel, then demo edit/save/reset/exit, followed by a byte-for-byte real database comparison. | `@claim:demo-isolation keeps a seeded real database unchanged`; live sentinel check. |
| R2-M5 — generic deep-link metadata | Retained route-specific metadata and included `/offline.html` with its own title, description, canonical, and social fields. | `all public pages share navigation, footer links, build id, and complete metadata`; live `/demo`, `/records`, `/guide`, and `/offline.html` checks. |
| R2-M6 — undersized touch targets | Kept 44 px app/legal/demo controls and made both offline recovery actions at least 44 × 44 px. | `all routes have no axe violations, horizontal overflow, or undersized chrome targets`; [live offline screenshot](evidence/polish-4/live/offline-fallback-mobile.png). |
| R2-M7 — inconsistent chrome and landmarks | The generated shared header/footer now covers app, legal, 404, and offline documents; all expose the same navigation, legal links, factory credit, and version. | `all public pages share navigation, footer links, build id, and complete metadata`; byte match confirms deployed documents. |
| R2-M8 — missing or broken CSP | Kept the strict same-origin policy, removed the offline page's inline style, and loaded only self-hosted `legal.css`/`legal.js`. | `test:build` rejects an inline offline style; live `/offline.html` and actual offline fallback produced no CSP console error. |
| R2-min9 — unrealistic sample reference | Retained the Northern Fulmar xeno-canto catalogue citation as inert text with licence and comparison notes. | `@claim:no-audio-fetch`; live demo and same-origin request checks. |
| F-3-1 — four raw h1 elements | Retained one route-specific raw and rendered h1 per route; added `/offline.html` to the structural check. | `route responses and rendered pages contain one route-specific h1`; `test:build`; live raw route audit. |
| F-3-2 — claims exceed proof | Kept narrowed public promises and 15 matching claims; the footer artwork wording and registry now agree exactly. | Claim registry unit test; 15/15 exact clean-clone commands; live copy/network audit. |
| F-3-3 — shell inconsistency | Extended the one generated shell to the offline fallback and updated all sources to `v1.0.4`. | Shared-shell build assertion across eight documents; live metadata/chrome audit; [offline screenshot](evidence/polish-4/live/offline-fallback-mobile.png). |
| F-3-4 — unexplained export formats | Retained the spreadsheet, shareable-text, and browser-migration explanations next to export actions and in README. | `@claim:exports`; live demo screenshot; clean CSV/Markdown/JSON export-and-import test. |
| F-4-1 — broken offline fallback | Rebuilt `/offline.html` as a CSP-safe field-instrument page with metadata, one h1, shared shell, legal links, version, and 44 px actions. The worker now checks its offline state before navigation, so an uncached URL receives this fallback. | `the installed app shell opens demo and legal routes offline`; shared route/axe/touch tests; [live fallback screenshot](evidence/polish-4/live/offline-fallback-mobile.png); live uncached navigation came from the service worker with zero console/axe errors. |
| F-4-2 — reachable jargon and vague states | Replaced all flagged strings: `Ready/Saved on this device`, specific start/delete dialog headings, `exported backup`, explicit import replacement text, `An update is ready`, and `Load update`. | `audits dialog, toast, error, offline, and import or export state copy`; `state messages use evidence-card terms and action-specific dialog headings`; [dialog screenshot](evidence/polish-4/local/new-card-dialog-mobile.png); live footer/status checks. |

## Final verification

- Clean clone `/tmp/bird-polish4-clean.8zNA83` at `ff0078b767cffde6bc6755080f6543a33c0bd395`: `npm ci`, 15/15 exact claim commands, then `npm test` all passed.
- Full clean suite: 10 unit tests; 45 browser tests across mobile and desktop; one intentional desktop skip for the phone-only first-screen assertion.
- Build budgets: JavaScript 10,651 bytes gzip; CSS 5,741 bytes gzip; mobile hero 14,501 bytes.
- Live deployment `b60c3431-eacc-44e5-ad48-9f644f740e6a`: seven 200 routes, designed 404, isolated demo, offline app routes, actual uncached fallback, CSP, axe, focus, touch targets, and privacy passed.
- All 28 publicly served build files matched `dist/` byte-for-byte. See [byte-match.json](evidence/polish-4/live/byte-match.json).
- Live Lighthouse: 100 performance, 100 accessibility, 100 best practices, 100 SEO; LCP 1,146 ms; CLS 0; TBT 0. See [Lighthouse result](evidence/polish-4/live/lighthouse.json).

No unresolved finding or known acceptance gap remains.
