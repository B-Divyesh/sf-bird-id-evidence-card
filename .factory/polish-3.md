# Polish 3 — cumulative finding closure map

Scope: release-candidate `4e7bca4aa5fe4721bc0dcf438b11c3a6dbdaaa08`, reviewed through `6e2cf52322e45be6bc3c364336421a2e4c148832`. Findings use `R1`, `R2`, and `F-3` prefixes for reviews 1, 2, and 3. No finding is deferred.

Final implementation commit tested from a clean clone: `5c8e7226807b91ba3f2807677080583605a018a1`. Live URL: <https://bird-id-evidence-card.sociobot.in/>.

| Finding | Change made | Evidence |
| --- | --- | --- |
| R1-B1 — unclear first screen | Replaced the metaphorical lead with the required six-word job headline, named birders and their situation, and placed the sample action beside its outcome and three facts. | `first phone screen states the job, audience, action, and outcome`; [home mobile screenshot](evidence/polish-3/home-mobile.png); live 390×844 check in [live audit](evidence/polish-3/live/live-audit.json). |
| R1-B2 — no isolated sample | Added one-click `?demo=1` and `/demo` entry, a realistic Deerness/Northern Fulmar sample, persistent demo banner, reset, blank-card exit, and the separate `demo:bird-id-evidence-card` database. Pending autosaves are cancelled before reset/exit. | `@claim:demo-isolation`; `@claim:offline-demo`; [demo screenshot](evidence/polish-3/demo-mobile.png); live sentinel/reset/exit result in [live audit](evidence/polish-3/live/live-audit.json). |
| R1-B3 — no claim registry | Added `.factory/claims.json` with 15 scoped claims and one uniquely tagged observable test per claim. Removed or narrowed public promises that could not be proved. | `tests/unit/claims.test.ts`; every registry command passed independently in clean clone `/tmp/bird-polish3-final.mukzQg`; [verification summary](evidence/polish-3/verification-summary.json). |
| R1-B4 — fake routes and 200 fallback | Added static route documents for `/demo`, `/records`, and `/guide`, History API navigation, route focus/announcement, and a designed host-level 404 with HTTP 404 status. | `route responses and rendered pages contain one route-specific h1`; `routes use meaningful titles and navigation restores focus`; live route/status checks in [live audit](evidence/polish-3/live/live-audit.json). |
| R1-H5 — incomplete metadata/chrome | Added route titles, descriptions, canonicals, OG/Twitter metadata, the 1200×630 product-art social image, SVG favicon, apple-touch icon, shared header/footer, factory attribution, and version `v1.0.3`. | `all public pages share navigation, footer links, build id, and complete metadata`; `scripts/verify-build.mjs`; live audit across all routes. |
| R1-M6 — jargon, length, and inconsistent terms | Rewrote landing/README copy in short sentences, consistently calls the saved object an `evidence card`, and limits implementation terms to developer instructions. | `.factory/copy-audit.md` has no over-22-word or banned-word flags; current `README.md`. |
| R1-M7 — unclear headings and controls | Replaced destination labels with verb-led links and made route/section headings meaningful in isolation. | route/focus browser test; axe heading checks; live navigation labels in [live audit](evidence/polish-3/live/live-audit.json). |
| R2-B1 — claim commands fail in a clean clone | Playwright now builds before previewing. The exact commands accept forwarded `--grep` arguments. Full runs use four fresh-browser shards to avoid a Chromium lifetime crash in the worker image. | All 15 exact commands passed independently after `npm ci` in `/tmp/bird-polish3-final.mukzQg`; `ALL_CLAIMS_PASS=15`. |
| R2-B2 — destinations remain off-screen | Each app destination displays only its route content, scrolls to the top, focuses its route `<h1>`, updates history, and restores focus on back/forward. | `routes use meaningful titles and navigation restores focus`; live keyboard/route-focus check. |
| R2-H3 — missing core/privacy claims | Registered recording, separation, non-identification, no-account, schema, deletion, same-origin/no-tracking, and artwork claims with outcome tests. | `@claim:record-evidence-card`, `@claim:separate-observation-and-suggestion`, `@claim:no-automatic-identification`, `@claim:no-account`, `@claim:delete-card`, `@claim:stored-card-schema`, `@claim:no-tracking-or-remote-assets`, and `@claim:generated-artwork`. |
| R2-H4 — weak demo-isolation proof | The demo test first saves and snapshots a real sentinel card, then edits, saves, resets, and exits demo before comparing the real database byte-for-byte. | `@claim:demo-isolation keeps a seeded real database unchanged`; repeated test passed 6/6; live audit repeats the sentinel flow. |
| R2-M5 — generic deep-link metadata | Static route shells and runtime navigation now provide route-specific titles, descriptions, canonicals, OG/Twitter fields, and product social art. | route metadata browser test; raw-response checks in `scripts/verify-build.mjs` and live audit. |
| R2-M6 — undersized targets | Enlarged demo controls, legal/footer links, recovery actions, and mobile navigation to at least 44×44 CSS px. | `all routes have no axe violations, horizontal overflow, or undersized chrome targets`; [live mobile screenshot](evidence/polish-3/live/screenshot-mobile.png). |
| R2-M7 — inconsistent chrome and landmark | App, demo, legal, and 404 documents use the same wordmark, four-link primary navigation, three-link footer, attribution, and build ID. The guide no longer nests a complementary landmark. | `all public pages share navigation, footer links, build id, and complete metadata`; live route audit. |
| R2-M8 — missing CSP | Added a strict same-origin CSP plus nosniff and referrer headers compatible with the service worker and local assets. | `scripts/verify-build.mjs`; live response-header assertions in `npm run test:live`. |
| R2-min9 — invalid sample reference | Replaced the fake personal-notes URL with a Northern Fulmar recording-catalogue reference, licence, and comparison note; the URL remains inert text. | `@claim:no-audio-fetch`; [full demo screenshot](evidence/polish-3/demo-full-mobile.png). |
| R2 copy audit | Replaced `Privacy shutter on`, `Decision`, `Live card readout`, `Field record`, `Start for real`, `Cancel`, and guide jargon with direct, consistent language. | `.factory/copy-audit.md`; first-screen and navigation tests. |
| F-3-1 — four raw `<h1>` elements | Build-time route generation now emits exactly one route-specific `<h1>` and demotes all subordinate headings. Runtime navigation applies the same outline. Saved-record titles are `<h2>` so populated archives also retain a valid outline. | `route responses and rendered pages contain one route-specific h1`; build verifier; live raw HTML checks on `/`, `/demo`, `/records`, and `/guide`; populated-route axe test. |
| F-3-2 — public claims exceed proof | Narrowed audio language to storage/no-fetch behavior; added no-account, valid-coordinate, delete, schema, and tracking claims/tests; removed the stronger clear-storage and cache/reproduction promises. | All 15 clean-clone claim commands passed; specifically `@claim:no-account`, `@claim:private-coordinates`, `@claim:no-audio-fetch`, `@claim:delete-card`, and `@claim:stored-card-schema`. |
| F-3-3 — app/legal/404 chrome differs | Build scripts source one header/footer definition for every app, legal, and 404 route; labels, destinations, factory credit, and `v1.0.3` now match. | `all public pages share navigation, footer links, build id, and complete metadata`; live audit covers all seven page types. |
| F-3-4 — file formats unexplained | Kept result-naming export buttons and added: CSV opens in a spreadsheet, Markdown is shareable text, and a backup moves all cards to another browser. | `@claim:exports` validates CSV, Markdown, JSON, and fresh-context import; current `README.md`; [full demo screenshot](evidence/polish-3/demo-full-mobile.png). |

## Final evidence

- Clean clone: `/tmp/bird-polish3-final.mukzQg` at `5c8e7226807b91ba3f2807677080583605a018a1`.
- Registry: all 15 exact `.factory/claims.json` commands passed independently.
- Full clean suite: 9 unit tests passed; 43 browser tests passed across phone and desktop; one deliberate desktop skip for the phone-only first-viewport assertion.
- Build verification: JavaScript 10,624 bytes gzip; CSS 5,743 bytes gzip; mobile hero 14,501 bytes.
- Live verification: [verify-url result](evidence/polish-3/live/verify.json) and [cold live audit](evidence/polish-3/live/live-audit.json) passed after deployment.
- Live Lighthouse: 100 performance, 100 accessibility, 100 best practices, 100 SEO; LCP 1,132 ms; TBT 0 ms; CLS 0. See [summary](evidence/polish-3/lighthouse-live-summary.json).

There are no deferred findings or known acceptance gaps.
